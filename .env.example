# Riot API key (server-side only)
RIOT_API_KEY=

# Vercel / Upstash persistent cache (free tier)
# Prefer the Vercel Redis integration values if you use the Vercel dashboard.
KV_URL=
KV_REST_API_TOKEN=

# Fallback: direct Upstash Redis values
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
